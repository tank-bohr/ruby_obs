#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_configure:
	autoconf
	dh_auto_configure -- --prefix=/opt/ruby-<%= package_version %> --disable-install-doc --with-jemalloc

override_dh_auto_test:
	true
