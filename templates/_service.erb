<services>
  <service name="download_url">
    <param name="protocol">http</param>
    <param name="host">cache.ruby-lang.org</param>
    <param name="path">/pub/ruby/<%= package_version %>/ruby-<%= ruby_version %>.tar.gz</param>
  </service>
</services>
